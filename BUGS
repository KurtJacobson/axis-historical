Known Bugs in AXIS
* jog buttons don't work if you tab to them and use the space bar
* (feature?) f-key actions can be done from MDI tab
* ctrl-k doesn't work in MDI tab
* "zero offset" probably doesn't work when metric is involved
* "override limit" keystroke shortcut not documented

Bugs in EMC that limit functionality in AXIS
* Can't step to first line of program
* Pressing "step" (t) twice quickly may run instead of stepping

Bugs in segmentqueue that affect AXIS
* Pressing "step" (t) twice runs instead of stepping (every time)
* Pressing "s" key (resume) while running leaves interpreter in the wrong state
